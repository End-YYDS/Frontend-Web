name: Frontend Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && startsWith(github.ref, 'refs/heads/debug/'))

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test --if-present

      - name: Build
        run: pnpm build

      - name: Archive dist
        id: archive
        run: |
          REF_NAME="${GITHUB_REF_NAME}"
          # 把 ref 裡面的 / 全部換成 -
          SAFE_REF_NAME="${REF_NAME//\//-}"
          ARCHIVE_NAME="frontend-${SAFE_REF_NAME}.zip"

          echo "Archive name: ${ARCHIVE_NAME}"

          # 輸出給後面 steps 用
          echo "archive_name=${ARCHIVE_NAME}" >> "$GITHUB_OUTPUT"

          cd dist
          zip -r "../${ARCHIVE_NAME}" .
        env:
          GITHUB_REF_NAME: ${{ github.ref_name }}
        shell: bash

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.archive.outputs.archive_name }}
          path: ${{ steps.archive.outputs.archive_name }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        with:
          tag_name: ${{ github.ref_name }}
          name: "Frontend ${{ github.ref_name }}"
          files: |
            ${{ steps.archive.outputs.archive_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
